{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/projOne",
            "cwd": "${workspaceRoot}/build/",
            "preLaunchTask": "build"
        }
    ]
}